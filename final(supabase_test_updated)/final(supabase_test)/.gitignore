node_modules/
dist/
__pycache__/
*.pyc
phishguard_log.txt

# Local secrets — copy .env.example to .env and fill in your keys.
# Flask loads .env at startup; the real file never ships.
.env
.env.local
