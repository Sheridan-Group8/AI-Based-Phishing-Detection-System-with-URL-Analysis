# PhishGuard runtime dependencies — direct imports only.
#
# Supported Python runtime: 3.10+.
#
# Version ranges use a lower bound high enough to include the most recent
# patched release in each stable minor, and an upper bound that forbids the
# next major (known-breaking). Keep this aligned with current advisory data
# and verify with `pip-audit --strict --requirement …`.
#
# For production builds you should produce a full transitive lockfile with
# hashes (e.g. `pip-compile --generate-hashes requirements.in`) and install
# with `pip install --require-hashes -r requirements.lock`. This file is the
# intent — the lockfile is the source of truth.

flask>=3.0.3,<4.0.0
requests>=2.33.0,<3.0.0 ; python_version >= "3.10"
numpy>=2.4.0,<3.0.0 ; python_version >= "3.14"
pandas>=3.0.0,<4.0.0 ; python_version >= "3.14"
scipy>=1.17.0,<2.0.0 ; python_version >= "3.14"
scikit-learn>=1.8.0,<1.9.0 ; python_version >= "3.14"

numpy>=2.0.0,<3.0.0 ; python_version < "3.14"
pandas>=2.2.0,<4.0.0 ; python_version < "3.14"
scipy>=1.13.0,<2.0.0 ; python_version < "3.14"
scikit-learn>=1.6.0,<1.7.0 ; python_version < "3.14"
